import { Colors } from '../colors';
import { TemplateType } from '../templateType';

export default {
  colors: {
    text: Colors.LIGHTGOLD,
    background: Colors.BROWN,
    templateType: TemplateType.LIGHT,
  },
};

