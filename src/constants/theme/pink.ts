import { Colors } from '../colors';
import { TemplateType } from '../templateType';

export default {
  colors: {
    text: Colors.WHITE,
    background: Colors.PINK,
    templateType: TemplateType.DARK,
  },
};
